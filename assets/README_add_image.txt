Place your background image here and name it `background.jpg`.

Recommended steps:
1. Save the provided image as `background.jpg`.
2. Copy/move it into the `weather-app/assets` folder.
3. Start the app (if using server):
   npm install
   npm start
4. Open http://localhost:3000 — the background will load automatically.

Notes:
- The CSS uses `assets/background.jpg` as the background image path.
- Use a web-friendly format (jpg, png, webp). If you use a large file, consider resizing for performance.
